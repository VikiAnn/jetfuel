<%= link_to "Add url", new_url_path %>
<% @urls.each do |url| %>
  <ul>
    <li><%= url.given_url %></li>
    <li>Shortened link: <%= link_to "jet.fuel/#{url.slug}", "/#{url.slug}" %></li>
  </ul>
<% end %>
